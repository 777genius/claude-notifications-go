# Installation design reference

Owner-selected visual reference: `installation-reference.png` (2026-09-10).

Match the centered heading, two agent cards, command panel, two aftercare cards,
and quiet footer links. No Back button. Display the browser-detected OS using
Bowser, with a manual override. Preserve supported installation/update/configure
flows, the real bootstrap command, Codex release prerequisite and hook trust
instructions. The image's example URL is illustrative and must not be used.

Place the compatibility strip between the hero and installation section.
Compare the built desktop screenshot with this image, and verify narrow screens.
