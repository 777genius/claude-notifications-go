# Plan §14 local E2E — 2026-09-11

Worktree `/private/tmp/notification-agent-notify-e2e`. Isolated `HOME`/`TMPDIR` under `/tmp/agent-notify-e2e-*`. `GOTOOLCHAIN=local`, `GOMAXPROCS=2`, `go test -p 1`. Exact-head helper: `swift-notifier/.build/arm64-apple-macosx/release/terminal-notifier-modern` SHA-256 `c1ef7778579228396dc55f958b926c74e61f49197cd3745e9886ba29d53c9f2a`.

This is a draft support matrix for locally proven capabilities. It is not a release, not Desktop live-model activation evidence, and not permission to publish.

`TestAgentNotifyIsolatedInstallFlowE2E` 2026-09-11 re-run with the helper present: **46.14s PASS**. Native A→B generations, `ClaudeNotifier.app` alias retarget, rollback inode reuse, retire-without-delete, direct `--capabilities-json`, and LaunchServices `open -a` of test bundle `com.agentnotify.test.flow` all executed. Notification Center send stays opt-in (`AGENT_NOTIFY_DARWIN_E2E=1`) and was **not** posted in this re-run. Production bundle `com.claude.desktop.notifier` is still not registered by the default flow. Isolated `HOME` does not confine OS databases.

Operator observation 2026-09-11 (earlier opt-in send): visible Notification Center banner from the isolated-flow silent send (`title=agent-notify e2e`). That confirms a banner can appear after `os_accepted`. It does not prove click routing to generation A, a Codex thread, or LaunchServices identity after update. The opt-in helper uses production bundle `com.claude.desktop.notifier`, so a click may be attributed to the installed notifier, not the published generation path.

## Claimed in this worktree

| Capability | Environment | Evidence |
| --- | --- | --- |
| existing-installer configure §8.1.2: explicit `--configure-notifications` from bootstrap/init/`setup-codex`, both client orders, primary command from ledger, partial second-client failure, no foreign clobber | isolated macOS fixture | Isolated flow: `proveConfigurePrimaryOrder`, `proveConfigureSecondClientFailure`. Entrypoints: `TestNotificationBootstrapOffline`, `TestNotificationInitOfflineBranch`, `TestNotificationConfigureParserAndSetupOptIn`, `TestSetupCodexE2EConfigureNotifications` (configure uses committed `$CODEX_HOME/claude-notifications-go` after source bundle removal). Bootstrap `install_codex` sets `CONFIGURE_BINARY` to that committed path and deletes staging before configure. |
| Production MCP tool surface from portable-launch after UAP projection; origin from `_meta.threadId`, not cwd/`clientInfo` | isolated UAP Codex projection, test executable as primary | `TestUAPProjectedCodexLaunchRunsProductionMCP`, `TestAgentNotifyIsolatedInstallFlowE2E` (`tools/list`, `notification_status`) |
| Notify A/B source targets after MCP death and cwd delete; same-cwd concurrent A/B; replay does not create a second effect; literal `--help [important]` preserved | in-process SDK + durable journal, injected delivery port | `proveNotifyTargetsAfterCwdGone` + `proveConcurrentSameCwdNotifies` inside `TestAgentNotifyIsolatedInstallFlowE2E`; `TestSDKServiceDurableReplayAndScopedOutcomes` |
| Native A→B unique generations; `bin/ClaudeNotifier.app` alias retargets without swapping callback inodes; generation A `--capabilities-json` after B; helper process cold-start via `open -a pathA` for `com.agentnotify.test.flow`; rollback restage of A reuses inode; retire does not delete published A/B | Darwin: exact-head helper when present, otherwise inert clang capability fixture so CI cannot skip the generation section. Default codesign `com.agentnotify.test.flow`. NC send and production-bundle LS remain behind `AGENT_NOTIFY_DARWIN_E2E=1` | `nativeFlowApp` / `inertNativeFlowApp`, `commitNativeWithHookAlias`, `assertHookAlias`, `launchGenerationAfterColdStart`, rollback/retire section in `TestAgentNotifyIsolatedInstallFlowE2E` |
| Portable reverse handoff restores owned Codex MCP/skill; locator gone; Claude/global/hooks unchanged; disable does not mutate them | isolated UAP | `TestHandoffReverseRefusesWhileLocatorExistsThenRestoresOwnedMCP`, isolated flow reverse + disable |
| Journal unknown/crash/replay fail closed | Go journal tests | `TestReplayIdentityAndCAS`, `TestActualCrashBoundaries` |
| Hook regression; disable new feature does not rewrite hooks | `./internal/hooks/` + isolated flow disable | `TestHandler_NotificationsDisabled`, isolated flow hook byte compare |
| Literal native protocol / delivery receipts | Go + shared fixtures | `TestPR3LiteralBytesAndBounds`, `TestPR3DeliveryLiteralReceiptAndSnapshot`, `TestLiteralAndReceipt` |
| Exact-head helper identity | local Swift release binary | `docs/evidence/agent-notify/draft-qualification-2026-09-11.md` |

## Unavailable / not proven here

| Item | Reason |
| --- | --- |
| Production tool selected by installed Codex Desktop live model | `/Applications/ChatGPT.app` is present. Handoff forbids quitting the working Codex, Codex CLI/app-server/subscription, and AppleScript/CDP UI bypass. No disposable Desktop profile in this worktree |
| Manual Notification Center A/B click on exact-head artifact after MCP death | Requires a user click in a dedicated test profile; warm spikes used synthetic metadata. Visible banner ≠ click routing |
| Queued Notification Center click after managed A→B update | Opt-in send-before-update (`AGENT_NOTIFY_DARWIN_E2E=1`) can yield `os_accepted` + a banner. Click routing after B is still a dedicated-profile manual gate because the opt-in helper shares `com.claude.desktop.notifier` with the installed notifier |
| Cold-start callback while Codex is fully quit | Helper process cold-start via explicit generation path (`open -a` of `com.agentnotify.test.flow`) is proven. Codex-quit cold-start needs a dedicated macOS test account/VM |
| Live portable client activation (Codex/Claude UI selected the package) | Projection + launcher + reverse proven; client activation is not claimed from synthetic listing |
| Publish / updater distribution | Draft only. Needs an explicit owner permission |

## Commands used

```sh
export GOTOOLCHAIN=local GOMAXPROCS=2 HOME=/tmp/agent-notify-e2e-home TMPDIR=/tmp/agent-notify-e2e-tmp
swift build -c release --arch arm64 --package-path swift-notifier
/opt/homebrew/bin/go test -p 1 -count=1 -timeout=180s ./cmd/claude-notifications/ -run 'TestAgentNotifyIsolatedInstallFlowE2E'
# optional operator-visible Darwin NC send / production-bundle LS:
# AGENT_NOTIFY_DARWIN_E2E=1 /opt/homebrew/bin/go test -p 1 -count=1 -timeout=180s ./cmd/claude-notifications/ -run 'TestAgentNotifyIsolatedInstallFlowE2E'
```
