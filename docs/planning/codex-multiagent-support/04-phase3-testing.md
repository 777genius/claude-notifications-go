# Фаза 3: тестирование и валидация

> [!WARNING]
> **ИСТОРИЧЕСКИЙ ДОКУМЕНТ. НЕ ИСПОЛЬЗОВАТЬ КАК АКТУАЛЬНЫЙ TEST PLAN.** Нормативная герметичная
> матрица находится в `00-overview-and-decisions.md`: отдельные temp `HOME`/`CODEX_HOME`, только
> test project, recording sinks и отсутствие записи за пределами sandbox.

Опирается на фазы 0-2 (все ревизии). Цель — закрыть все research-item'ы, оставленные предыдущими фазами как "подтвердить перед релизом", и дать реализуемый тест-план по слоям.

## 3.1 — Research-items, которые ОБЯЗАНЫ быть закрыты до релиза (не тестовый код, а фактическая проверка)

Собрано из ревизий фаз 0-2, ничего не придумано заново:
1. **Формат `last_assistant_message`** (1.3/1.7 фазы 1): сырой вывод модели или Codex-обёртка? Влияет на truncate-логику и code-fence поведение.
2. **Формат Codex `session_id`/`thread_id`** (1.7 фазы 1): hex-UUID (совместимо с `sessionname.GenerateSessionName` как есть) или ULID/иное (нужен FNV-фолбэк)?
3. **Семантика `stop_hook_active`** (1.6 фазы 1): подтвердить, что `true` значит "continuation-повтор, уже уведомляли" на реальном Codex, не что-то другое.
4. **Наличие CLI-поверхности у hooks** (2.2 фазы 2): есть ли `codex hooks --help`/аналог, или `/hooks` строго TUI-only — определяет, работает ли автодетект вообще или падаем сразу на интерактивный вопрос.
5. **Механика `/hooks` trust** (2.6 фазы 2): точные шаги, чтобы не печатать юзеру непроверенный UI-флоу.
6. **PermissionRequest — реальные условия срабатывания**: подтвердить (или опровергнуть) утверждение "не фаерится при bypassPermissions/dontAsk/--ask-for-approval never" — сейчас это ChatGPT-документация, не код (см. остаточный риск SDK-фазы).
7. **Легаси notify payload** (2.5 фазы 2): точная форма полей (`thread-id` vs `session_id` и т.д.) — сейчас список полей из памяти проекта, не верифицирован для этого захода заново.

**Метод закрытия**: (а) точечный веб-ресёрч/чтение codex-rs исходников — дёшево, сделать ДО написания кода фаз 1-2, не после; (б) где веб/исходники не дают ответа — единственный источник истины это 3.4 (живой Codex). Явно пометить в реализации, какие из 7 пунктов закрыты (а) и какие ждут (б) — не блокировать разработку по пунктам, закрываемым (б), но не релизить, пока они не подтверждены.

## 3.2 — Unit-тесты: `internal/codexhook` (декодер)

Мирроring стиля, каким тестировался SDK-декодер (уже проверенный паттерн, 5 ревью): успешный decode (полный payload), decode с отсутствующими опциональными полями (zero-value), malformed JSON, `stop_hook_active=true/false`, `tool_input` как произвольный JSON (не только `shell`-форма). **Дополнительно, специфично для hand-rolled версии** (в отличие от SDK, где лимит платежа был предметом спора и убран) — тест на большой (но НЕ отклоняемый, согласно ревизии 1.1/1.2) payload, подтверждающий, что decode не падает и не режет молча.

## 3.3 — Unit-тесты: классификация и плюмбинг

- `analyzer.ClassifyMessage`: по одному кейсу на каждое из 5 правил (empty→unknown, error/limit-фразы→session_limit/api_error, `Contains("?")`→question, plan-маркеры→plan_ready, иначе→task_complete) + edge cases из ревизии 1.3 (сообщение целиком код → пусто после CleanMarkdown → unknown; план, заканчивающийся на "?" → question, не plan — тест, фиксирующий намеренный приоритет).
- Config plumbing (1.5 ревизия): тест, что `GetStatusInfo("permission_request")` не возвращает ошибку (регрессионный тест именно на баг, который поймал критик — забытую запись роняла `SendDesktop` молча); тест на `isTimeSensitiveStatus("permission_request") == true`; тест на `summary.go:88`-ветку (permission_request даёт НЕ task-complete текст).
- `dedup`: тест двухфазного вызова (`CheckEarlyDuplicate`→`AcquireLock`) с Codex-ключом `(sessionID, "Stop")`, подтверждающий переиспользование без изменений пакета (как заявлено в ревизии 1.6).
- `sessionname.GenerateSessionName` на реальном формате Codex ID (после закрытия research-item 3.1 п.2) — если ULID/не-hex, тест на FNV-фолбэк вместо `Sscanf`.
- `textutil.CleanMarkdown` (перенесённая, ревизия 1.4) — тест, что перенос НЕ изменил поведение (те же assertions, что были в `summary_test.go` для `CleanMarkdown`, теперь в `textutil_test.go`); `emojiPattern` остался в `summary`, тест `GetDefaultMessage` не сломан.

## 3.4 — Unit-тесты: инсталлятор (Go table-driven, по прецеденту `install_e2e_test.sh`, но для Go-логики merge)

По находке критика фазы 2 (`bin/install_e2e_test.sh`/`install_test.sh`/`mock_server.py` — существующий харнесс, но bash-уровня для shell-инсталлятора; для Go-уровня merge-логики нужны отдельные Go-тесты в tmpdir):
- Merge в пустой/несуществующий `hooks.json` — создание с нуля.
- Merge в существующий hooks.json с чужими (не owner-marked) записями — чужие не тронуты.
- Повторный `codex-install` (идемпотентность) — owner-marked записи заменены на канонические (ревизия 2.3/2.4: canonical-reset, не "не трогать руками правленное").
- Malformed hooks.json — отказ, файл не тронут, чёткая ошибка.
- Content-hash guard: смоделировать конкурентную запись между read и rename (тест должен реально вставлять запись файла в середине процесса, не просто мокать) — подтвердить abort+retry срабатывает.
- Windows rename-retry: тест на `ERROR_SHARING_VIOLATION`-подобную ситуацию (открытый файловый хендл во время rename) — платформенно-условный тест (build tag `windows`).
- `codex-uninstall`: owner-marked записи убраны, чужие остались, backup удалён ТОЛЬКО после успешной верификации (ревизия 2.7) — тест, что backup СОХРАНЯЕТСЯ при смоделированном сбое верификации.
- TOML-путь (2.5 ревизия): текстовая вставка `notify=[...]` перед первым `[table]`-заголовком (не в конец файла) — тест на конфиг с секциями И без секций; тест, что комментарии/форматирование остального файла не изменились (byte-diff вне нашего добавленного блока).
- Legacy-uninstall: `notify`-запись убрана из config.toml хирургически, остальное содержимое byte-identical.

## 3.5 — Live/manual e2e тест-план (обязателен перед релизом, не автоматизируется полностью)

Реальный Codex CLI, реальная установка через `codex-install`:
1. Свежая установка на систему без `~/.claude/` вообще (чистый Codex-only юзер) — подтвердить `MkdirAll`-цепочку работает на каждом уровне (lock-файл, config.json, hooks.json).
2. `/hooks` trust flow — подтвердить реальные шаги, обновить текст 2.6, если разошёлся с реальностью.
3. Триггер `Stop` на реальной Codex-сессии — уведомление приходит, звук играет, click-to-focus поднимает правильное окно/pane.
4. Триггер `PermissionRequest` (реальный tool-call, требующий approval) — уведомление приходит с корректным `tool_name`; отдельно — подтвердить/опровергнуть research-item 3.1 п.6 (bypassPermissions/dontAsk и правда не фаерят событие).
5. Continuation-цикл (Codex несколько раз продолжает после Stop) — подтвердить `stop_hook_active`-подавление не даёт дублей, но и не глотает легитимные отдельные Stop.
6. Повторная установка (идемпотентность) на уже настроенной системе — hooks.json не растёт, записи заменяются, не дублируются.
7. Установка на систему БЕЗ поддержки hooks.json (если удастся найти/собрать старый Codex, иначе — форсировать legacy-путь флагом для теста) — legacy notify работает, PermissionRequest корректно НЕ приходит с явным предупреждением при установке (2.6 legacy-ветка).
8. `codex-uninstall` — hooks.json/config.toml возвращаются к состоянию без наших записей, чужие записи (если тестово добавить сторонний хук) не пострадали.
9. Гонка "install во время запущенного Codex" (намеренно, для проверки content-hash guard) — подтвердить видимый retry, не тихий hang, не порча файла.
10. Windows-специфично: символических путей нет — подтвердить `.exe`-путь в hooks.json работает через реальный `/hooks` trust (это первый раз, когда non-symlink стабильный путь проверяется вживую).

## 3.6 — CI-интеграция

- Unit-тесты (3.2-3.4) — в стандартный `go test ./...`, без live-Codex зависимости (все моки/fixtures).
- Live e2e (3.5) — по прецеденту repotests в plugin-kit-ai: gated по env var (`CLAUDE_NOTIFICATIONS_RUN_CODEX_CLI=1` или аналог), не блокирует обычный CI-прогон, документирован как ручной/опциональный gate перед релизом.

## РЕВИЗИЯ после 3 критиков фазы 3

### 3.1 — 5 из 7 research-items ЗАКРЫТЫ реальным веб-ресёрчем (p3-critic-research, с источниками)

**Метод (а) сработал для 5 пунктов, не был чисто теоретическим** — критик реально сходил в веб и codex-rs-производные источники и закрыл:
- **П.1 (last_assistant_message формат) — ЗАКРЫТО**: plain nullable string, поле `last_assistant_message`, на `Stop` И `SubagentStop`, доставляется STDIN. Не Codex-обёртка — сырой текст модели. Источники: learn.chatgpt.com/docs/hooks (официальная дока), pkg.go.dev/github.com/ajbeck/scut/hooks/codex (независимый Go-декодер, совпадающие struct-теги).
- **П.2 (session_id формат) — ЗАКРЫТО, ОБРАТНО моей гипотезе**: **hex-UUID (UUIDv7)**, НЕ ULID. Реальные примеры (`019b4087-c66f-7f93-8e19-f26a7b79cd90`). **Важно**: FNV-фолбэк в 1.7 Фазы 1 НЕ становится обязательным — остаётся защитным. (Также техническая поправка: `Sscanf(hex,"%x")` даже на non-hex не паникует и не "ломается" — парсит ведущий hex-run и возвращает низкоэнтропийное число, без ошибки — мой и p1-критика тезис "ULID сломает Sscanf" был неточен вдвойне, но уже неважно, раз формат подтверждённо hex.)
- **П.3 (stop_hook_active семантика) — ЗАКРЫТО, план был прав**: true = "следующая попытка после заблокированного стопа / предотвращение continuation-цикла", идентично Claude Code.
- **П.4 (CLI-поверхность hooks) — ЗАКРЫТО, я был неправ**: **`codex hooks --help` НЕ существует**. Конфигурация только файлами, trust только TUI `/hooks`. Полностью меняет стратегию детекта Фазы 2 §2.2 — см. аддендум там.
- **П.7 (легаси notify payload) — ЗАКРЫТО с точными полями**: kebab-case, ARGV (не stdin): `type=agent-turn-complete, thread-id, turn-id, cwd, client(опционально), input-messages, last-assistant-message`.

**Не закрыто — честно, не от нехватки времени**:
- П.5 (/hooks trust механика) — принцип понятен (auto-discover → юзер доверяет через TUI), точный текст шагов реально ждёт живого Codex (метод (б) сработал как задумано).
- П.6 (PermissionRequest под bypass-режимами) — НЕ подтверждено НИ ОДНИМ источником, логически выводимо, но не документировано нигде — genuinely требует живого Codex или более глубокого дайва в codex-rs.

**Вывод**: порядок "ресёрч → код → тест" НЕ сломан — 5/7 закрываются заранее, ровно 2 (5,6) ждут живого Codex, как метод (б) и предполагал.

### НОВОЕ, не входившее в исходные 7 пунктов (критично, ест обратно в Фазы 1-2)

**A1 — два разных wire-формата, план их недостаточно разделял**: hooks-путь (snake_case, stdin, `last_assistant_message`) и legacy-путь (kebab-case, argv, `last-assistant-message`) — РАЗНЫЕ механизмы. `internal/codexhook` (1.1 Фазы 1) описывает ТОЛЬКО hooks-путь; `handle-codex-notify` (2.5 Фазы 2) — отдельный декодер. Зафиксировано явно в обеих ревизованных фазах (уже внесено).

**A2 — version-gated feature flag + Windows-неопределённость**: ранние версии Codex поставляли hooks как experimental/off-by-default (`[features] codex_hooks = true` в config.toml, deprecated-алиас `hooks` в новых), один источник — "недоступно на Windows". **Внесено как блокирующий research-item в аддендум Фазы 2** — Windows-специфичный код (стабильный `.exe`-путь, Windows rename-retry) не писать до подтверждения.

### 3.2 — добавлены обязательные, evidence-backed тест-кейсы
- **Non-ASCII/UTF-8/эмодзи в `last_assistant_message`** — не гипотетика: реальный открытый баг **openai/codex#23784** ("Stop hook receives malformed JSON stdin on Windows with non-ASCII assistant message"). Обязательный тест, привязан к Windows-специфичному риску.
- `hook_event_name` дискриминация `Stop` vs `SubagentStop` (оба несут одинаковый набор полей).
- `transcript_path` как JSON null vs absent vs present (nullable).
- Явное разделение: тесты 3.2 покрывают ТОЛЬКО hooks-путь (`internal/codexhook`); legacy-путь (`handle-codex-notify`, kebab/argv) — отдельные тесты, не смешивать в одном разделе (A1).

### 3.3 — `sessionname`-тест переписан (был спроектирован под неверную гипотезу)
Убрать "если ULID/не-hex — тест FNV-фолбэк" — session_id подтверждён UUIDv7. Заменить на: тест с реальным примером UUIDv7 (`019b4087-c66f-7f93-8e19-f26a7b79cd90`), assert детерминированный не-"unknown" результат. Опционально — один adversarial non-hex-input тест, документирующий no-crash-поведение `Sscanf` (defensive, не потому что ожидается такой input).

### 3.4 — тестовая архитектура сделана implementable (p3-critic-installer-tests, с кодом)

**Content-hash guard тест НЕДЕТЕРМИНИРУЕМ без явного production test-seam** — план должен МАНДАТИТЬ инъектируемый hook в merge-функции, срабатывающий строго между hash и rename (не горутина+`time.Sleep`, будет флейковым):
```go
type hookMerger struct { afterHash func() } // nil в проде; тест пишет файл здесь
func TestMergeHooks_ConcurrentWriteAbortsAndRetries(t *testing.T) {
    // afterHash пишет "внешнего писателя" один раз → assert retry сработал ≥2 раз,
    // финальный результат содержит И наши записи, И конкурентную правку (не затёрта)
}
```
Модель угрозы уточнена: гонка — с ВНЕШНИМ не-локирующим процессом (сам Codex/юзерский редактор), не со вторым нашим инстансом (тот сериализован flock'ом).

**Windows rename-retry — разделить на два теста, не один**: (a) детерминированный, кросс-платформенный LOGIC-тест с инъектируемой `renameFn`, фейлящей N раз по КОНКРЕТНОМУ errno-предикату (`ERROR_SHARING_VIOLATION`/`ERROR_ACCESS_DENIED`, matched через `*os.LinkError`→`syscall.Errno`, не строкой) — таблица `{failN, maxN, wantCalls, wantErr}`; (b) реальный OS-level тест, `//go:build windows`, best-effort, **`t.Skip()`, не `t.Fail()`**, если сценарий не воспроизвёлся детерминированно (антивирус/индексатор — не мы). (b) переносится в 3.5/3.6 (не 3.4 — недетерминирован по своей природе).

**"byte-diff вне блока" — верно ТОЛЬКО для TOML, НЕ для hooks.json**: TOML (текстовая вставка) → byte-golden fixture в `testdata/` реализуем и правилен. hooks.json (parse→merge→marshal) → re-marshal нормализует порядок ключей/отступы — byte-diff даст ложные падения; нужна **структурная** проверка (unmarshal оба, deep-equal чужого поддерева + каноничность наших записей).

**Underspecified, требует пиннинга до написания тестов**: owner-marking механизм (уже уточнён в ревизии Фазы 2 — токен-подстрока `--owner=`, не exact-match — тестируемо); "malformed hooks.json" — два отдельных теста (невалидный JSON vs валидный-но-неожиданной-формы); TOML-без-секций — вставка в конец файла, если заголовков `[table]` нет вообще (уточнено в ревизии Фазы 2); hermeticity — installer-тесты через `HOME`-override ИЛИ path-injection DI (НЕ реальный `~/.codex`, в отличие от teamstate-тестов, которые пишут в реальный `statePath()` с `defer os.Remove` — так для инсталлятора нельзя); `HOME`-override процессо-глобален → content-hash тест НЕ должен быть `t.Parallel()`.

### 3.5 — прецедент НЕ покрывает то, ради чего его цитировали (p3-critic-e2e-ci, реально прочитал файл)

**Блокер**: `plugin-kit-ai/repotests/codex_cli_notify_e2e_test.go` (34 теста, реально прочитан) **НИКОГДА не трогает hooks.json и не фаерит ни одного hook-события** — все ассерты по легаси `notify=[...]`-механизму через `-c notify=<override>` в `codex exec`. Доказывает только, что легаси-путь можно гонять headless. **Про hooks.json-сценарии (№3-5 ниже) НЕ говорит ничего** — добавлен НОВЫЙ research-item в 3.1: фаерит ли `codex exec` вообще hooks.json-события, или это TUI/interactive-session-only.

**Раскладка 10 сценариев по классам (план их не различал)**:
- **Класс A (чистые unit-тесты инсталлятора, Codex-рантайм НЕ нужен, ДУБЛИРУЮТ 3.4)**: №1 (свежая установка), №6 (идемпотентность), №8 (uninstall), №9 (content-hash race) — **перенести в 3.4 как Go-тесты**, убрать из "live e2e".
- **Класс B (автоматизируемо через `codex exec`, обосновано прецедентом)**: №7 (легаси notify) — прямое 1:1 переиспользование паттерна `codexNotifyOverride`+`runCodexExec`.
- **Класс C (автоматизируемо ТОЛЬКО если подтвердится, что `codex exec` фаерит hooks — сейчас UNPROVEN)**: №3 (частично — сам факт срабатывания хука; звук/click-to-focus принципиально manual, GUI), №4 (approval policy interactive, headless не форсируется детерминированно — в основном manual), №5 (недетерминированное поведение модели, флейки — лучше как unit-тест, уже покрыто 3.3's `stop_hook_active`-dedup, убрать из live).
- **Класс D (принципиально manual, человек в TUI/GUI)**: №2 (`/hooks` trust — подтверждено, скриптовать нельзя), №10 (Windows non-symlink путь через тот же TUI trust).

**Безопасность (реальный риск)**: №9 против РЕАЛЬНОГО запущенного Codex может испортить настоящий `~/.codex/hooks.json` разработчика — прецедент НИКОГДА не пишет в реальный `~/.codex/` (инжектит конфиг через `-c notify=` + `t.TempDir()`/`--ephemeral`). №9 — только sandboxed (fake HOME + stub-codex/инъектируемый writer, как в 3.4's content-hash тесте), НИКОГДА против живого Codex-процесса.

### 3.6 — CI-гейтинг переписан под реальность (Codex e2e физически не может быть обычной CI-джобой)

Codex-e2e требует ЗАЛОГИНЕННОГО Codex + реального ПЛАТНОГО вызова модели — на GitHub-hosted раннере без секретов не запустится в принципе. Репо УЖЕ имеет свои прецеденты для этого класса тестов (план ошибочно копировал только plugin-kit-ai's паттерн, не заметив свои):
- `install_e2e_test.sh --real-network` с `continue-on-error: true` + `timeout-minutes: 5` на конкретной Go-версии матрицы — для сетезависимого, не блокирующего CI.
- `notifier-signing-smoke.yml` — `workflow_dispatch` (ручной триггер) — для дорогого/секрет-зависимого прогона.

**Новая стратегия**: класс B (№7, легаси) — env-var-gated ЛОКАЛЬНЫЙ opt-in (имя переменной зафиксировать, ПЛЮС SKIP-побратим `..._SKIP_CODEX_CLI=1`, как в прецеденте — принудительно выключать, если codex случайно оказался в PATH раннера), модель через `-codex-model` флаг (не хардкод, ротация моделей), `--ephemeral` обязателен. **Обязательный трёхслойный skip** (не fail) по прецеденту `codexBinaryOrSkip`: env-флаг → бинарь в PATH → `codex login status` ок → иначе `t.Skip()`; плюс skip на нездоровый рантайм. В обычном CI — максимум `workflow_dispatch`, никогда обычная джоба.

**Release-process gap (найден, не в исходном плане)**: `docs/RELEASE.md` существует, нумерованный чеклист 1-7, шаг 3 = `make test-race, make lint` — сейчас там НЕТ никакого live/e2e-гейта. План заявлял 10 сценариев "обязательны перед релизом", не говоря КТО их гоняет и ЧТО блокирует тег. **Добавить явный шаг в `docs/RELEASE.md`** (между текущими 3 и 4): "Codex live e2e — releaser гоняет ручной чеклист (класс D: №2/4/5/10) + env-gated класс B (№7); результат фиксируется в release-PR/issue; тег не создаётся без отметки."

## Итоговые решения фазы 3 (готово для финального сквозного ревью)

- 5/7 research-items закрыты ресёрчем, 2 (п.5,6) ждут живого Codex — порядок ресёрч→код→тест не сломан.
- Два wire-формата (hooks/legacy) — раздельные декодеры/тесты, зафиксировано в Фазах 1-2.
- Content-hash/Windows-retry тесты требуют явного production test-seam — иначе нереализуемы по тексту плана.
- 10 "live"-сценариев → 4 в класс A (переехали в 3.4 как unit), 1 в класс B (автоматизируемо), 3 в класс C (в основном manual, UNPROVEN что hooks вообще фаерятся через `codex exec`), 2 в класс D (принципиально manual).
- CI: только class B локально/`workflow_dispatch`-gated, никогда обычная джоба; добавлен шаг в `docs/RELEASE.md`.
