# Фаза 4: документация и релиз

> [!WARNING]
> **ИСТОРИЧЕСКИЙ ДОКУМЕНТ. НЕ ИСПОЛЬЗОВАТЬ КАК АКТУАЛЬНЫЙ RELEASE PLAN.** Нормативные release
> gate, selector, Go 1.22 floor и native marketplace contract находятся в
> `00-overview-and-decisions.md`.

Опирается на фазы 0-3 (все ревизии). Самая механическая фаза — почти все решения уже приняты, здесь — где именно они фиксируются письменно и как встраиваются в существующий релизный процесс.

## 4.1 — README.md

- Новая секция "Codex CLI support" (по образцу существующей структуры документации Claude-фич) — что устанавливается, какие события покрыты (`Stop`, `PermissionRequest`), явно — какие НЕ покрыты (остальные 10 lifecycle-событий Codex, вне scope v1).
- Установка: `codex-install`/`codex-uninstall` — команды, что они делают, ссылка на обязательный шаг `/hooks` trust.
- **Явная секция "Known limitations"**: PermissionRequest недоступен на legacy-notify-пути (старый Codex/неподтверждённый Windows); список research-items, которые остались неподтверждёнными на момент релиза (если есть — например, п.6 из Фазы 3, PermissionRequest под bypass-режимами).
- Таблица конфигурации — новые ключи (`codex`-секция в `config.json`, если добавлена в 1.5).

## 4.2 — docs/CLICK_TO_FOCUS.md (или аналогичный документ)

- Codex-специфичная секция: наследование env-переменных терминала хук-процессом Codex — то же самое, что у Claude (по итогам живого теста Фазы 3 §3.5, п.3) — задокументировать реальный результат, не предположение.
- Если живой тест выявит расхождение (например, Codex спавнит хук через демон/detached-процесс, ломая env-наследование) — задокументировать ограничение явно, не молчать.

## 4.3 — docs/RELEASE.md

**Уже частично специфицировано в ревизии Фазы 3 (§3.6)**: новый шаг между существующими 3 ("make test-race, make lint") и 4 ("commit, push, wait for CI") — "Codex live e2e: releaser гоняет ручной чеклист (класс D сценариев из Фазы 3 — №2/4/5/10) + env-gated класс B (№7, легаси notify); результат фиксируется в release-PR/issue; тег не создаётся без отметки о прогоне."

Дополнительно для ЭТОГО релиза (первого с Codex-поддержкой) — разовый пункт, не постоянный: подтвердить все research-items, помеченные "ждёт живого Codex" в Фазе 3 §3.1 (п.5, п.6), ПЕРЕД тем как снять пометку "experimental"/"beta" с фичи в README.

## 4.4 — CHANGELOG.md

По образцу уже установленного в этом же репо формата (см. недавний релиз v1.41.0 этого чата) — секция "Added" с описанием Codex-поддержки, честно помечено как первичная/beta-реализация (не "полная поддержка Codex", раз охвачено только 2 из 12 событий, и часть research-items не закрыта живым тестом на момент первого релиза).

## 4.5 — CONTRIBUTING/внутренняя документация (если применимо к этому репо — проверить, есть ли аналог)

- Зафиксировать письменно решения, которые иначе живут только в этом планировочном документе и в head реализатора: frozen argv-контракт (`handle-codex-hook Stop|PermissionRequest --owner=claude-notifications-go`) — **пометить явно как "НЕ МЕНЯТЬ без понимания последствий для trust-хэша Codex-юзеров"**, аналогично тому, как в этом самом репо уже есть предупреждение про `sh`-префикс в hook-командах (CLAUDE.md текущего проекта).
- Аналогично: canonical-reset owner-marked записей, verify-then-delete-backup порядок в uninstall — не самоочевидные инварианты, которые легко случайно сломать в будущем PR, если не записаны.

## 4.6 — Версионирование и релизный флаг

- Codex-поддержка — MINOR-версия bump (новая функциональность, backward-compatible — существующие Claude-пользователи не затронуты).
- Обсудить (не решать здесь — решение пользователя): маркировать релиз как "Codex support (beta)" в GitHub Release notes, учитывая неполное покрытие research-items и единственный живой тест-прогон перед первым релизом.

## РЕВИЗИЯ после 3 критиков фазы 4

### 4.1/4.4/4.6 — позиционирование переписано (критик: "beta" без прецедента и слишком расплывчато)

**Факт**: в этом репо НЕТ прецедента "beta"/"experimental"-лейбла вообще (grep = 0). Вводить его — новая, непонятная юзеру конвенция. Плюс: Windows-ситуация — это функциональный пробел (PermissionRequest физически недоставим на legacy-фолбэке, сама поддержка hooks не подтверждена), не "шероховатости" — заслуживает явной формулировки, а не общего hedge.

**Новый подход — platform/mechanism-specific формулировки вместо одного лейбла**:
- README: "Codex CLI support (new in vX.Y)" + секция **"Known Limitations"**: Windows — "не протестировано; поддержка hooks в Codex на Windows не подтверждена, при отсутствии — фолбэк на legacy notify, который не может доставлять permission-уведомления."
- **Новая секция README "Troubleshooting → Not getting Codex notifications?"** (критик дал точный список пунктов, включая САМЫЙ коварный — не входивший в исходные 7 research-items — неподтверждённое "фаерит ли `codex exec` вообще hooks-события"): (1) выполнен ли `/hooks` trust; (2) permission-уведомления приходят только когда Codex РЕАЛЬНО спрашивает разрешение, не в auto/bypass режимах; (3) нужна интерактивная сессия. **Критичное условие формулировки**: пункты 2 и "не-интерактивный `codex exec` не фаерит hooks" — сами НЕ подтверждены, формулировать как "ожидаемое поведение, требует подтверждения", не как факт.
- **4.6 — историческая рекомендация**: в release notes нельзя заявлять проверенную платформу заранее. Формулировка строится только из exact-head evidence; неподтверждённые платформы перечисляются в Known Limitations.
- Мелкие правки атрибуции: формат CHANGELOG — установленная конвенция ВСЕГО репозитория (Keep a Changelog+SemVer), не "формат из этого чата"; "11 lifecycle-событий Codex, 2 покрыты" — внешний, неподтверждённый счёт, смягчить формулировку ("остальные события Codex, вне scope v1") вместо жёсткого числа в публичном README.

### 4.3 — релизный гейт: список сценариев исправлен + гейт стал условным (критик нашёл, что план сам себе противоречил)

**Критичная находка**: список сценариев гейта ({№2,4,5,10}) противоречил собственной таксономии Фазы 3 — №4/№5 на деле класс C, не D; №5 **прямо противоречит** решению Фазы 3 убрать его из live (флейки, уже покрыт unit-тестом); и, главное, **гейт полностью пропускал №3 — само ядро фичи** (реально ли уведомление появляется на живом Stop, звук, click-to-focus). Как было написано, релиз мог пройти гейт, ни разу не подтвердив, что уведомление вообще приходит.

**Исправленный список гейта**: `{№2, №3, №10}` (принципиально ручные GUI/TUI-сценарии) + класс B `№7` (env-gated, автоматизируемо). №4 — по возможности, необязателен. №5 — исключён окончательно (уже в unit-тестах §3.3).

**Гейт — условный, не безусловный** (критик: проверил последние 4 реальных релиза этого репо — ни один не был про Codex; требовать живой Codex e2e перед КАЖДЫМ релизом ради несвязанного фикса — гарантированная десенсибилизация галочки именно к моменту, когда она важна): условие — механически детектируемое, `git diff vLAST..HEAD --name-only` пересекает `internal/codexhook/`, `handle-codex-*`, инсталлятор-пути. Если пересекает — гейт обязателен; если нет — пропускается явно, не молча.

**Место фиксации исправлено** (критик: "release-PR" в реальном флоу этого репо НЕ существует — шаг 4 `docs/RELEASE.md` это прямой `git commit`+`push`, без PR): фиксировать в теле release-коммита (trailer) или строке CHANGELOG-записи, не в несуществующем release-PR.

**Опциональный, не обязательный harder-механизм**: довесить в уже существующий CI-job `verify-version` (единственный реальный automated checkpoint в `release.yml`) детекцию тех же путей — если Codex-пути менялись и в коммит-трейлере нет маркера `Codex-e2e: <note>`, job падает с понятным сообщением. Честная оговорка (сам критик подчеркнул): CI не может проверить, что живой прогон РЕАЛЬНО был (нет логина/секретов/платного вызова на раннере) — это speed-bump/напоминание, не доказательство сути. Для solo-репо (подтверждено по авторам коммитов) — пропорционально; не городить GitHub Environment+required-reviewers (over-engineering, ревьюеров в репо нет).

**Точка вставки vs точка enforcement** — синхронизированы: чекбокс явно привязан к шагу 5 (`git tag`), не только "между 3 и 4".

### 4.5 — полностью переписан: 3 инварианта → 45, разделённые по механизму защиты (DOC vs CODE)

**Критик прочитал все 4 документа фаз полностью и нашёл ~45 инвариантов** против исходных 3 в этом разделе. Ключевая структурная поправка, определяющая весь раздел: **doc-заметка защищает АРХИТЕКТУРНОЕ решение ("не заводить второй config-путь"), но НЕ защищает конкретную строку кода, которую будущий рефакторинг случайно заденет** ("никто не читает CONTRIBUTING.md перед тем как слить два похожих на вид хендлера"). Поэтому: DOC-инварианты → новая секция в `docs/ARCHITECTURE.md` (существующий файл, 431 строка — правильный дом для "архитектурного why", подтверждено критиком; НЕ изобретать аналог CONTRIBUTING.md, он уже есть) + одна строка-указатель в `CLAUDE.md` в стиле уже существующего sh-prefix-предупреждения (`CLAUDE.md:7`, подтверждено дословно). CODE-инварианты → инлайн-комментарий ПРЯМО В ТОЙ ЖЕ строке/функции, добавленный **в том же PR**, что пишет эту строку — не отложенная документация.

**Уточнение прецедента**: `AGENT_CRITICAL_GUARDRAILS.md`, на который ссылается родительский workspace `CLAUDE.md`, **физически отсутствует** по указанному пути в этом чекауте (проверено критиком) — ссылка из workspace-заметок сейчас висит в пустоте для этого конкретно репо; не полагаться на него как на формат-прецедент здесь.

**Полный список по тирам (severity-first, из отчёта критика; для каждого — источник и механизм защиты)**:

**Tier 1 — ломает trust-хэш** (молча: у ВСЕХ существующих юзеров хуки перестают фаериться до ручного ре-trust): (1) frozen argv-строка целиком [BOTH]; (2) путь бинаря в hooks.json обязан быть стабильным/неверсионированным — отдельная от (1) половина контракта, ранее не выделена явно [BOTH]; (3) имена событий без префикса "Codex" — намеренно иначе, чем в SDK [DOC]; (4) `handleCodexHook` читает только `os.Args[2]`, никогда `flag.Parse()` — иначе `--owner=` может быть случайно съеден "чисткой" аргументов [CODE, обязателен инлайн].

**Tier 2 — молчаливая потеря данных/порча** (файлы юзера, чужие конфиги, без отката): (5) content-hash guard, не mtime+size [BOTH]; (6) backup — точные байты hash-снимка, не свежее перечтение [CODE]; (7) verify-then-delete-backup порядок, backup остаётся по умолчанию [BOTH]; (8) owner-поиск — токен-подстрока, не exact-match [BOTH]; (9) canonical-reset ВСЕГДА, но раздельно frozen command-string vs resettable metadata [BOTH]; (10) TOML — только хирургическая вставка, никогда decode+reencode [CODE]; (11) TOML footgun с `[table]`-секциями [CODE + тест уже в §3.4]; (12) **`state.go:176` намеренно БЕЗ default для `permission_request` — обратный инвариант**, кто-то "доделывающий" switch добавит ветку и permission-уведомления начнут молча попадать под question-cooldown [CODE, обязателен инлайн]; (13) legacy-uninstall тоже убирает `notify` из `config.toml` [BOTH].

**Tier 3 — молчаливая деградация фичи** (неверное поведение на новом пути, без видимой ошибки): (14) **`handleCodexHook` — осознанно отдельная функция, не DRY-ить с `handleHook`** — самый вероятный случайный слом во всём плане [CODE, обязателен на ОБЕИХ функциях]; (15) контракт вывода — exit 0/пустой stdout/лог в файл, никогда `errorhandler.HandlePanic` [BOTH]; (16) порядок правил классификации — error/limit ПЕРЕД task_complete-дефолтом [CODE+тест+DOC]; (17) question-правило перед plan-правилом [CODE]; (18) `Contains("?")`, не suffix [CODE]; (19) 6 обязательных config-plumbing мест, не 5 — особенно `summary.go:88` [CODE+тест]; (20) отсутствие записи в `Config.Statuses` роняет `SendDesktop` молча — регрессионный тест первичен [тест-first]; (21) `isTimeSensitiveStatus permission_request → true` [CODE/тест]; (22) **таймаут hooks.json = 30, не 25 — 25 это отдельная константа `maxNotifyDelaySeconds`, обязана оставаться ниже** — cross-file magic-number связка [CODE на ОБОИХ местах, перекрёстная ссылка]; (23) dedup-окно 2с, не 2-5с [CODE]; (24) двухфазный dedup обязателен [CODE/тест]; (25) два раздельных dedup-механизма, не один [CODE]; (26) `hook_event_name` из JSON — не источник истины, decode не должен реджектить на несовпадении [CODE]; (27) **лимит payload сознательно УБРАН — не добавлять `io.LimitReader`** для Codex stdin [CODE, обязателен инлайн — единственный `LimitReader` в репо для чужого HTTP, не для локального stdin]; (28) **`emojiPattern` остаётся в `summary`, только `CleanMarkdown` переезжает в `textutil`** [CODE, обязателен инлайн — рефакторинг-ловушка]; (29) два wire-формата — раздельные декодеры, не унифицировать DTO [BOTH]; (30) legacy notify не может доставить PermissionRequest — обязательно и в README, и в install-выводе [DOC]; (31) `WithLock` обязан `MkdirAll` — teamstate никогда этого не задевал [CODE]; (32) лок-путь в `~/.claude/...`, не unlink на разлоке [CODE]; (33) **`codex-uninstall` использует ТОТ ЖЕ лок-путь, что `codex-install`** [CODE — разделяемая константа] (34) отдельный лок-путь для `config.toml` [CODE]; (35) Windows retry — errno-матчинг через `*os.LinkError`, не строка [CODE+тест]; (36) Windows-путь — `.exe` напрямую, не `.bat`/симлинк [CODE].

**Tier 4 — косметика/дисциплина скоупа/честность**: (37) `question.mp3` переиспользуется, не новый ассет [DOC/CODE]; (38) FNV-фолбэк в sessionname — защитный, не обязательный (session_id подтверждён UUIDv7) [CODE]; (39) content-hash тест — инъектируемый seam, не goroutine+sleep, НЕ `t.Parallel()` [CODE+тест-комментарий]; (40) byte-diff golden валиден только для TOML, не hooks.json [тест-комментарий]; (41) installer-тесты — HOME-override/DI, никогда реальный `~/.codex` [тест-комментарий+DOC]; (42) историческое ограничение Go 1.21 отменено, нормативный floor — Go 1.22 [DOC]; (43) 2 research-items всё ещё неподтверждены на живом Codex — не снимать "experimental" до закрытия [DOC]; (44) `codex exec` уже подтверждён для Stop hook, открытым остаётся детерминированный PermissionRequest [DOC]; (45) релизный гейт между шагами 3-5 `docs/RELEASE.md` — сам критик fase 4.3 отметил риск "чеклист выродится в пропуск", см. ревизию 4.3 выше про условность+опциональный CI-маркер.

## Открытые вопросы для критиков фазы 4

1. 4.1/4.4 — честно ли предлагаемое позиционирование "beta"/"experimental" для первого релиза, или это излишняя осторожность/недостаточная осторожность, учитывая объём неподтверждённого (Windows-поддержка hooks, 2 research-items, единственный ручной e2e-прогон)?
2. 4.3 — реалистичен ли предлагаемый релизный gate (ручной чеклист перед каждым релизом, трогающим Codex-код) в долгосрочной перспективе, или он выродится в "все всегда пропускают эту галочку" — нужен ли более жёсткий механизм (например, CI явно блокирует релиз без отметки, не просто чеклист в доке)?
3. Что из решений Фаз 0-3 упущено здесь и должно быть зафиксировано письменно, но не зафиксировано?
4. Что вообще упущено в этой фазе?
